[user]
  name = Peter Thesling
  email = peter.thesling@shopify.com
[alias]
  st = status
  ap = !git add . && git commit --amend --no-edit && git push origin +HEAD
  rb = !git checkout main && git pull && git checkout - && git rebase main
